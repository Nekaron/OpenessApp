<Window x:Class="OpenessApp.Views.PreConfigurationEnvironmentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:OpenessApp.Behaviors"
        Title="Module Pre-Selection" Height="400" Width="550"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <i:Interaction.Behaviors>
        <behaviors:WindowCloser DialogResult="{Binding DialogResult}" />
    </i:Interaction.Behaviors>
    <DockPanel LastChildFill="True" Margin="10">
        <!-- Pfad -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Module Path:" VerticalAlignment="Center" />
            <TextBox Text="{Binding ModulePath, UpdateSourceTrigger=PropertyChanged}"
                     Width="350" Margin="5,0" VerticalContentAlignment="Center"/>
            <Button Content="…" Width="30" Command="{Binding BrowsePathCommand}"/>
        </StackPanel>

        <!-- Module -->
        <ListBox ItemsSource="{Binding Modules}" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Content="{Binding AssemblyName}" Width="200"/>
                        <TextBlock Text="{Binding EngineeringDll}" Width="200" Margin="10,0"/>
                        <TextBlock Text="{Binding VersionInfo}" Width="60"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Bestätigung -->
        <Button Content="Confirm" Height="30" Width="100"
                HorizontalAlignment="Right" Margin="0,10,0,0"
                Command="{Binding ConfirmCommand}" IsDefault="True"/>
    </DockPanel>
</Window>
